# Copy to .env and adjust.
# Base URL of the backend (FastAPI). Used by the dev proxy and the app at runtime.
# Leave empty to keep requests relative and rely on the Vite dev proxy.
VITE_BACKEND_URL=http://127.0.0.1:8000
